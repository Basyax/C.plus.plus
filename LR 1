#include <iostream>
#include <cmath>

using namespace std;

int main() {
    // Вхідні дані
    double a = 3.0, b = 0.501;

    // Перевірка області визначення логарифма
    if (a - b <= 0) {
        cout << "Помилка: логарифм не визначений для a - b <= 0" << endl;
        return 1; // Завершити програму з помилкою
    }

    // Обчислення чисельника
    double part1 = pow((a - b), 2); // (a - b)^2
    double part2 = sin((1 - a / b) * M_PI / 3); // sin((1 - a/b) * π/3)
    double part3 = cos((1 - b / a) * M_PI / 3); // cos((1 - b/a) * π/3)

    double numerator = cbrt(part1 * pow(part2, 2) * pow(part3, 2)); // Корінь третього ступеня

    // Обчислення знаменника
    double denominator = 0.701 * pow(log(a - b), 2); // 0.701 * ln(a - b)^2

    // Обчислення y
    double y = numerator / denominator;

    // Вивід результату
    cout << "Значення y: " << y << endl;

    return 0;
}
