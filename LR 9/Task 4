#include <iostream>   // Підключаємо бібліотеку для вводу/виводу
#include <vector>     // Підключаємо бібліотеку для роботи з векторами

using namespace std;  // Використовуємо стандартний простір імен

int main() {
    int n;  // Оголошуємо змінну для розміру масиву
    int A;  // Оголошуємо змінну для числа A

    // Вводимо розмір масиву
    cout << "Введіть кількість елементів масиву: ";
    cin >> n;  // Зчитуємо розмір масиву

    vector<int> m(n);  // Створюємо динамічний масив (вектор)

    // Вводимо елементи масиву
    cout << "Введіть елементи масиву: ";
    for (int i = 0; i < n; i++) {  
        cin >> m[i];  // Зчитуємо кожен елемент масиву
    }

    // Вводимо число A
    cout << "Введіть число A: ";
    cin >> A;  // Зчитуємо число A

    // Лічильник для елементів, більших за A
    int count = 0;  // Ініціалізуємо лічильник нулем

    // Перебираємо елементи масиву
    for (int i = 0; i < n; i++) {  
        if (m[i] > A) {  // Якщо елемент більший за A
            count++;  // Збільшуємо лічильник
        }
    }

    // Виводимо результат
    cout << "Кількість елементів масиву, більших за " << A << ": " << count << endl;

    return 0;  // Завершуємо програму
}
